# Copy to /etc/hormuz-dock.env (chmod 600). Referenced by the systemd unit.
PORT=4100
DATA_DIR=/opt/hormuz-dock/data
# Only used to seed the 'admin' account on the very first run. Use a strong value.
ADMIN_PASSWORD=change-me
